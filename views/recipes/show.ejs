<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Recipes</title>
  </head>
  <body>
    <h1><%= recipe.name %></h1>
    <p>World Cuisine: <%= recipe.worldCuisine %></p>
    <% if (recipe.readyToEat) { %>
    <p>Ready to Eat 👍🏽</p>
    <% } else { %>
    <p>Not Ready to Eat 👎🏽</p>
    <% } %>
  </body>
</html>
